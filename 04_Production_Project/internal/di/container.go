package di

// TODO: Dependency Injection Container
// Responsibilities:
// - Keep `main.go` extremely clean
// - Wire Database -> Repositories -> Services -> Handlers
// - Ensure Singleton patterns for costly items (e.g., 1 DB Connection pool)
// - Utilize simple Constructors or frameworks like `google/wire`
