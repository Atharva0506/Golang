package http

// TODO: HTTP Delivery Layer (Controllers)
// Responsibilities:
// - Parse incoming HTTP requests (JSON bind, query params)
// - Pass requested data to the injected Service Layer
// - Format domain model responses cleanly back into JSON
// - Handle error mapping (Domain Errors -> HTTP Status Codes)
